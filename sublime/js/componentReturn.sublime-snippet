<snippet>
  <content><![CDATA[
${1:export }const ${2:C} = ({ ${3:className} }) => {
    return (
        <div />${4}
    );
};
]]></content >
  <description>Component (return)</description>
  <tabTrigger>COMPRET</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <!-- <scope >source.python</scope > -->
</snippet >
